{
  "name": "@bjm/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-cutting infrastructure (logging, errors, Kafka helpers, outbox, health) shared by services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bjm/contracts": "*",
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/swagger": "^8.15.0",
    "@fastify/swagger-ui": "^4.2.0",
    "fastify": "^4.28.0",
    "kafkajs": "^2.2.4",
    "pino": "^9.5.0",
    "prom-client": "^15.1.3",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
